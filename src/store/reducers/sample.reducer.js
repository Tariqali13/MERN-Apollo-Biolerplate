import {} from "../actions";

const initialState = {};

const SampleReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export { SampleReducer };
